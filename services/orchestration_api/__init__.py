"""CLAIRO U5 Orchestration/API service.

Contains the Claim API handler, the DynamoDB-Streams orchestrator that chains the
agent pipeline, the routing evaluator, and the human review task manager.
"""
